name = "nexus-pages"
compatibility_date = "2024-01-01"
pages_build_output_dir = "public"

[vars]
# TURSO_URL と TURSO_AUTH_TOKEN は
# Cloudflare Dashboard > nexus-pages > Settings > Environment variables
# に Secret として登録すること
