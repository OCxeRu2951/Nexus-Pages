{
  "name": "nexus-pages",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev":    "wrangler pages dev public --compatibility-date=2024-01-01",
    "deploy": "wrangler pages deploy public --project-name=nexus-pages --branch=main"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
